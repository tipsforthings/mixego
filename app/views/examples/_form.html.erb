<%= simple_form_for(@example) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :check %>
    <%= f.input :it %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
