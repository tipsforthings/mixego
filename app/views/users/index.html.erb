<% provide(:title, 'All users') %>
<h1>All users</h1>
<div class="row">
  <div class="medium-10 medium-offset-1 columns">
<%= foundation_paginate @users %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>
<br />
<%= foundation_paginate @users %>
</div>
</div>
